<% if current_user.mylist_by?(post_question.id) %>
    <%= link_to 'マイリストを解除',"/mylist/destroy/#{post_question.id}" ,method: :delete, remote: true %>
<% else %>
    <%= link_to 'マイリスト登録', "/mylist/create/#{post_question.id}", method: :post, remote: true %>
<% end %>