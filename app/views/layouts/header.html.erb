<div class="sb-nav-fixed">
    <nav class="navbar navbar-expand navbar-dark bg-dark">
    <% if current_user %>
        <a class="navbar-brand" href="/posts">capsule study</a>
    <% elsif %>
        <a class="navbar-brand" href="/">capsule study</a>
    <% end %>
    <% if !current_user %>
    <div class="menu">
        <a class="navbar-brand" href="/login">ログイン</a>
        <a class="navbar-brand" href="/new">新規登録</a>
    </div>
    <% elsif %>
        <button class=" btn btn-link btn-sm order-1 ml-2" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation" ">
            <i class=" fas fa-bars"></i>
        </button>
        <!-- Navbar-->
        <ul class="navbar-nav ml-auto">
            <li class="nav-item dropdown">
                <div class="dropdown">
                    <a class="btn btn-dark dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%= image_tag(current_user.image.thumb.url) %>
                    </a>
                    <div class="dropdown-menu bg-light" aria-labelledby="dropdownMenuLink">
                        <a class="dropdown-item" href="/users/show/<%= current_user.id %>">マイページ</a>
                        <a class="dropdown-item" href="/logout">ログアウト</a>
                    </div>
                </div>
            </li>
        </ul>
    </nav>
    <div class="collapse navbar-collapse bg-light" id="navbarTogglerDemo01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="btn btn-light nav-link" href="/questions/new">問題を解く</a>
            </li>
            <li class="nav-item">
                <a class="btn btn-light nav-link" href="/posts">投稿する</a>
            </li>
            <% if current_user.admin? %>
                <li class="nav-item">
                    <a class= "btn btn-light nav-link" href="/questions/index">問題一覧/作成</a>
                </li>
            <% end %>
            <p class="mt-2 ml-3">現在のポイント</p>
            <li class="nav-item ml-5">
                <p><%= current_user.point %>P</p>
            </li>
        </ul>
    </div>
    <% end %>
</div>

<script>
    function delteData(num) {
        if (confirm('本当に退会しますか？')) {
            document.location = "/users/delete/" + num;
        }
        return false;
    }
</script>